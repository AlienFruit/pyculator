# План рефакторинга обработчиков действий

## 📋 Общая цель

Унифицировать обработку действий: переместить все уведомления из обработчиков UI-событий в методы бизнес-логики (`handle_*`), чтобы обеспечить согласованное поведение независимо от способа вызова (горячая клавиша или кнопка тулбара).

---

## 🔧 План работы

### 1. Сохранение файла (Save File)

#### 1.1. Изменения в `app.py`
- [x] Добавить уведомление "Файл сохранен" в метод `handle_save_file()` после успешного сохранения
- [x] Убрать уведомление из метода `_on_ctrl_s_global()` (оставить только вызов `handle_save_file()`)
- [x] Проверить обработку ошибок в `handle_save_file()` (должна быть согласована)

#### 1.2. Проверка
- [ ] Проверить, что уведомление появляется при сохранении через Ctrl+S
- [ ] Проверить, что уведомление появляется при сохранении через кнопку 💾 в тулбаре
- [ ] Проверить обработку ошибок при сохранении

---

### 2. Создание файла (Create File)

#### 2.1. Изменения в `app.py`
- [x] Добавить уведомление "Файл {file_name}.py создан" в метод `handle_create_file()` после успешного создания
- [x] Убрать уведомление "Создание нового файла..." из метода `_on_ctrl_n_global()` (оставить только вызов `handle_create_file()`)
- [x] Убедиться, что уведомление показывается только при успешном создании файла

#### 2.2. Проверка
- [ ] Проверить, что уведомление появляется при создании через Ctrl+N
- [ ] Проверить, что уведомление появляется при создании через кнопку 📄 в тулбаре
- [ ] Проверить обработку ошибок при создании файла

---

### 3. Выполнение кода (Run Code)

#### 3.1. Анализ необходимости уведомления
- [x] Решить, нужно ли уведомление "Код выполнен" (результат виден в панели вывода)
- [x] Если уведомление не нужно - убрать его из `_on_f5()` в `python_editor.py`
- [x] Если уведомление нужно - добавить его в метод `handle_run_code()` в `app.py`

#### 3.2. Изменения в `python_editor.py`
- [x] Убрать уведомление "Код выполнен" из метода `_on_f5()` (если решено переместить в бизнес-логику)
- [x] Оставить только вызов `run_code_callback()`

#### 3.3. Изменения в `app.py` (если уведомление нужно)
- [x] Добавить уведомление "Код выполнен" в метод `handle_run_code()` после успешного выполнения (решено не добавлять, результат виден в панели вывода)

#### 3.4. Проверка
- [ ] Проверить выполнение кода через F5
- [ ] Проверить выполнение кода через кнопку ▶ в тулбаре
- [ ] Убедиться, что результат отображается в панели вывода

---

### 4. Удаление файла (Delete File)

#### 4.1. Проверка текущей реализации
- [x] Убедиться, что уведомление "Файл удален" находится в `handle_delete_file()` (уже правильно)
- [ ] Проверить, что уведомление появляется при удалении через кнопку ✖ в тулбаре
- [x] Убедиться, что нет обработчика горячей клавиши для удаления (или он тоже вызывает `handle_delete_file()`)

---

### 5. Дополнительные проверки и улучшения

#### 5.1. Унификация обработки ошибок
- [ ] Проверить согласованность обработки ошибок во всех методах `handle_*`
- [ ] Убедиться, что ошибки обрабатываются единообразно (через `Toolbar.show_error()` или `Notification.show()`)
- [ ] Проверить, что сообщения об ошибках информативны и понятны

#### 5.2. Проверка архитектуры
- [x] Убедиться, что все обработчики UI-событий (`_on_*`, `_handle_*`) только вызывают методы бизнес-логики
- [x] Проверить, что все уведомления находятся в методах `handle_*`
- [x] Убедиться, что нет дублирования логики между обработчиками

#### 5.3. Тестирование
- [ ] Протестировать все действия через горячие клавиши
- [ ] Протестировать все действия через кнопки тулбара
- [ ] Убедиться, что поведение идентично в обоих случаях
- [ ] Протестировать обработку ошибок для всех действий

---

## 📝 Примечания

- Все изменения должны следовать принципу: **UI-обработчики только вызывают бизнес-логику, уведомления - часть бизнес-логики**
- После каждого изменения проверять работу через оба способа вызова (горячая клавиша и кнопка)
- Удаление файла уже реализовано правильно - использовать его как эталон для остальных действий

---

*План создан на основе анализа из `save_notification_analysis.md`*  
*Дата создания: 28.11.2025*

