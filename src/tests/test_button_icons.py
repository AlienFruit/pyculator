#!/usr/bin/env python3
"""Тест отображения иконок на кнопках."""

import customtkinter as ctk
from components.toolbar import Toolbar
from components.file_panel import FilePanel
import os

def test_button_icons():
    """Тестирование отображения иконок на кнопках."""
    root = ctk.CTk()
    root.title("Тест иконок кнопок")
    root.geometry("600x200")

    # Создаем тулбар
    toolbar = Toolbar(root)

    # Создаем простую панель файлов для демонстрации
    file_panel = FilePanel(root, initial_directory=os.getcwd())

    # Добавляем информацию
    info = ctk.CTkLabel(
        root,
        text="Иконки в приложении:\n\n📄 Создать файл | 💾 Сохранить | ▶️ Выполнить код\n\n📁 Выбрать директорию (в панели файлов)\n\nНаведите курсор на кнопки для подсказок!",
        font=ctk.CTkFont(size=11),
        justify="left"
    )
    info.pack(pady=10)

    print("Тест иконок запущен!")
    print("Кнопки теперь показывают иконки вместо текста.")
    print("Иконка выполнения центрирована с пробелами.")

    root.mainloop()

if __name__ == "__main__":
    test_button_icons()
