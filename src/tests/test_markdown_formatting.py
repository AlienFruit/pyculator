#!/usr/bin/env python3
"""Тест визуального оформления Markdown элементов."""

import customtkinter as ctk
from components.output_markdown import MarkdownOutputDisplay

def test_markdown_formatting():
    """Тестирование визуального оформления markdown."""
    # Создаем главное окно
    root = ctk.CTk()
    root.title("Тест визуального оформления Markdown")
    root.geometry("800x600")

    # Создаем экземпляр MarkdownOutputDisplay
    output_display = MarkdownOutputDisplay(root)

    # Добавляем тестовый контент
    test_markdown = """
# Основной заголовок

Это обычный текст с **жирным** и *курсивным* форматированием.

## Подзаголовок второго уровня

### Подзаголовок третьего уровня

## Списки:
- Первый элемент списка
- Второй элемент списка
- Третий элемент списка

Нумерованный список:
1. Первый нумерованный элемент
2. Второй нумерованный элемент
3. Третий нумерованный элемент

## Код:
```python
print("Hello, World!")
for i in range(3):
    print(i)
```

### Инлайн код:
Используйте функцию `print()` для вывода текста.

**Жирный текст** и *курсивный текст* для выделения важных моментов.
"""

    output_display.append_markdown(test_markdown)

    # Добавляем инструкции
    instructions = ctk.CTkLabel(
        root,
        text="🎨 Визуальные маркеры для различения элементов:\n\n📌 Заголовки:\n• # ЗАГОЛОВОК → ==> ЗАГОЛОВОК <== (заглавные буквы)\n• ## Подзаголовок → → Подзаголовок\n• ### Подподзаголовок → - Подподзаголовок -\n\n✨ Форматирование:\n• **Жирный** → ●Жирный● (с точками)\n• *Курсив* → ‹Курсив› (с угловыми скобками)\n\n💻 Код:\n• `инлайн код` → выделенный цветом\n• ```блоки кода``` → в рамке ┌─ Код ─┐\n\n📝 Списки:\n• - Элемент → • Элемент (с точкой)\n• 1. Элемент → 1. Элемент (без изменений)",
        font=ctk.CTkFont(size=11),
        wraplength=750,
        justify="left"
    )
    instructions.pack(pady=10)

    print("Тест визуального оформления запущен!")
    print("Заголовки теперь выделены специальными текстовыми маркерами.")
    print("Жирный и курсивный текст имеют визуальные индикаторы.")

    # Запускаем главный цикл
    root.mainloop()

if __name__ == "__main__":
    test_markdown_formatting()
