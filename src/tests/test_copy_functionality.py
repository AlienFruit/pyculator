#!/usr/bin/env python3
"""Тест функциональности копирования текста в MarkdownOutputDisplay."""

import customtkinter as ctk
from components.output_markdown import MarkdownOutputDisplay
import time

def test_copy_functionality():
    """Тестирование копирования текста."""
    # Создаем главное окно
    root = ctk.CTk()
    root.title("Тест копирования текста")
    root.geometry("600x400")

    # Создаем экземпляр MarkdownOutputDisplay
    output_display = MarkdownOutputDisplay(root)

    # Добавляем тестовый контент
    test_markdown = """
# Тест копирования текста

Это **жирный текст**, а это *курсив*.

## Списки:
- Первый элемент
- Второй элемент

## Код:
```python
print("Hello, World!")
for i in range(3):
    print(i)
```

### Таблица:
| A | B | C |
|---|----|---|
| 1 | 2 | 3 |
| 4 | 5 | 6 |
"""

    output_display.append_markdown(test_markdown)

    # Добавляем инструкции
    instructions = ctk.CTkLabel(
        root,
        text="Инструкции для копирования:\n1. Нажмите кнопку '📋 Копировать' в заголовке\n2. Щелкните правой кнопкой мыши для контекстного меню\n3. Нажмите Ctrl+C для копирования выделенного текста\n4. Ctrl+A для выделения всего текста",
        font=ctk.CTkFont(size=12),
        wraplength=550,
        justify="left"
    )
    instructions.pack(pady=10)

    print("Тест копирования и выделения запущен!")
    print("Добавлен тестовый markdown контент.")
    print("Попробуйте:")
    print("1. Кнопка '📋 Копировать' в заголовке")
    print("2. Правая кнопка мыши -> 'Копировать весь текст'")
    print("3. Ctrl+C для копирования")
    print("4. Двойной клик для открытия окна с выделяемым текстом")
    print("5. Вставьте скопированный текст в любой редактор")

    # Запускаем главный цикл
    root.mainloop()

if __name__ == "__main__":
    test_copy_functionality()
